@model PhaseBannerViewModel
@if (Model!.ShowBanner)
{
    <div class="govuk-phase-banner">
        <p class="govuk-phase-banner__content">
            <strong class="govuk-tag govuk-phase-banner__content__tag">
                @Model.Status
            </strong>
            <span class="govuk-phase-banner__text">
                This is a new service – your <a class="govuk-link" rel="noreferrer noopener" target="_blank" href="@Model.Url">feedback</a> will help us to improve it.
            </span>
        </p>
    </div>
}
